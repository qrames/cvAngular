import { StyleByCategoryDirective } from './style-by-category.directive';

describe('StyleByCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleByCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
